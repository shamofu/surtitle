# Implementation and verification status

CI guidance updated 2026-09-17. Development is on main. Dated verification records below retain their original measurements and context. This document distinguishes implemented behavior, observed tests, and unfinished release requirements.

Following the user's approval to proceed with the [product redesign](transcribe-product-reconsideration-2026-09-12.md), [Study a draft](draft-study.md) now supports available ranges, source-block text, contextual replay, local bookmarks and confirmed excerpts with immutable audio cards. A whole-track problem does not block unrelated study. Original warnings, responses and cost reservations remain intact. The [verification record](draft-study-verification-2026-09-12.md) distinguishes fixed-response tests, saved real responses, package checks and remaining observations. New recognition mechanisms remain hypotheses; historical quality results below are unchanged.

The latest [English dialogue partial pilot](transcribe-en-dialogue-2026-09-12.md) executed the separately approved six Transcribe requests once. All returned valid nonempty subtitles and settled for USD 0.040962, with no new holds; cumulative charges and retained holds are USD 1.313156. The uncorrected provisional full-source WER is 22.41% for the current profile and 18.46% for the short candidate, including simultaneous speech and unresolved boundaries. There are respectively one and two unresolved boundary conflicts. Incomplete timing coverage and matched-subset p95 of 570/600 ms do not establish quality acceptance. No profile was promoted and no further request was sent.

The preceding [completion follow-up](completion-followup-2026-09-12.md) adds independently verified upstream references and complete native learning export/restore coverage. Review identified and fixed a restore archive replacement race with a bounded private snapshot, single-use preview tokens and cancellation/startup cleanup. The final Linux run passed 86 UI tests, 296 Rust workspace tests and 20 real Tauri E2E cases; the final Windows run passed 30 native E2E cases plus six focused restore regressions. The final normal application and unsigned NSIS build passed the strengthened sixty-resource package audit. Container isolation retained zero host output writes and no volumes. The other evaluation cells and human listening remain incomplete.

The preceding [Transcribe recovery implementation](transcribe-implementation-2026-09-12.md) added immutable local range revisions, explicit no-speech confirmation, stale-edit protection, retained unknown holds and adoption without further sending. Its policy reports automatic joining as an improvement target. That Linux run passed 83 UI tests, 289 Rust workspace tests, both explicit FFmpeg checks and 19 real Tauri E2E cases. The original 24-request, 33-minute-36-second pilot remains frozen; the separate reference-complete English candidate does not silently replace it. Long natural-speech quality remains unqualified. Historical observations below retain their original counts and scope.

The preceding fresh Windows run passed 29 native E2E cases, eleven transcript-command tests and two real FFmpeg tests. The latest normal binaries exclude development validation and fixed fixtures, as recorded in the completion follow-up. Neither the ordinary production profile nor installer lifecycle was exercised in this follow-up.

The [September 12 quality work](quality-improvement-2026-09-12.md) adds bounded transcript evidence and explicit local reparse, configurable source-playback context, contextual explanation prompts, and separately approved evaluation campaigns. The local Whisper experiment failed its acceptance criteria and is not integrated. The [subsequent twenty-request diagnosis](ai-diagnosis-2026-09-12.md) returned nineteen results and one HTTP 429, retained without retry. Flash invented speech on both non-speech controls; four of five received explanations need correction. Transcribe performed better in the short diagnostic samples, but long-chunk and boundary qualification remains incomplete. Historical paid results below remain unchanged and are distinct from this new evidence.

## Implemented workflows

- Tauri 2, React, TypeScript, TanStack Router/Query/Virtual, Japanese/English UI, light/dark themes, and persisted appearance preferences.
- Local video/audio, SRT/VTT, embedded text-subtitle extraction and version recovery, direct media downloads, and public single-video YouTube VOD import. Live, playlist, and authenticated downloads remain outside scope.
- Windows libmpv child-window playback, metadata without FFmpeg, speed/seek/range stop/repeat, tracks, fullscreen, subtitle refresh, and saved position/audio-stream restoration after file readiness. Optional caption-group pauses use cue-aligned punctuation/gap boundaries; explicit source ranges and repeats take priority. Draft study suspends inferred caption stops without changing the saved preference.
- Range-local draft bookmarks, text/audio confirmation, optional separately approved AI help, immutable cards and selected-range export. Invalid provider timing remains inspectable as a source block; no word times are invented. Restored bookmarks retain reference text and bounds without operational job bindings or confirmation authority.
- Virtualized subtitle search/edit/follow controls, translations, pointer/keyboard follow suspension, source-range playback, and explicit subtitle adoption. Vocabulary candidates replay their complete source-cue group. Missing, reordered, or nonadjacent selected cues require reselection; stale selections cannot silently shrink or retain an old loop.
- Per-tool Managed/External selection, FFmpeg/ffprobe pairing, duplicate PATH candidates, simple Scoop-shim resolution, capability probes, job-time versions/hashes, upstream CLI updates, rollback, and active-version retention. Startup discovery only caches unverified candidates; explicit selection probes the chosen installation. Managed update checks run at startup and every 15 minutes while open, with a 24-hour successful-result cache and a manual bypass. Metadata is bound to the selected channel and installed identity; installation is always explicit. Download jobs retain a receipt identifying the tools actually used. External installations are never updated or deleted by the app.
- Selected-stream FFmpeg/Silero/ORT preparation with bounded memory, exact sample ranges, overlap-aware estimates, immutable receipts, progress/cancellation, and prepared-audio reuse.
- Rust-only DPAPI credentials; Google model discovery plus arbitrary Gemini ID input; unset per-purpose defaults and per-job model/settings overrides. No application model catalog or quality allowlist.
- Optional public/manual price snapshots; zero initial monetary budgets; explicit priced or unpriced-scope approval; persistent reservations; one outgoing request; crash recovery; no automatic retry or model switch; distinct unpriced usage and unknown outcomes.
- Source-validated vocabulary, proficiency-aware explanations, translation, local application of received results, retained transcript originals/conflicts, inspectable automatic-join provenance, VAD no-speech and within-chunk pause warnings, digest-bound review/adoption, and separately approved boundary repair. New preparation receipts bind strict low-posterior pause evidence from the existing Silero inference pass; warnings preserve the original text and times.
- Immutable card source cues, consistent source example/translation/audio, card editing/suspension/deletion, media removal without deleting originals/cards, FSRS at 90% retention, and in-app review reminders. Again ratings can become due in the open review page; schedule revisions prevent stale snapshots from resubmitting an already rated review.
- CSV/TSV, source/translated SRT/VTT, JSON, and JSON/audio ZIP; validated restore with a pre-restore backup. Playback polling, media changes, and restore share a coordinator so stale native state cannot overwrite the restored position. Retained media reloads its archived position, audio, subtitles, and sentence boundaries; absent media detaches safely. Learning restore does not import cost ledgers, credentials, paid jobs, approvals, or external-tool selections.
- JSON/ZIP export enforces restore-compatible limits and replaces the destination only after the complete file is synced. Missing registered card audio fails ZIP export with card details; an incomplete backup cannot silently succeed. See [data transfer](data-transfer.md).
- Background download progress, targeted cancellation, explicit retry, incomplete-file cleanup, and library refresh after a successful download.
- Development-only validation CLI, independent result/reference hashes, automated metrics and AI-review rubric, and offline production-engine transcript stitching.
- Shared-source Dev Container with container-only dependencies/build outputs and no named or anonymous volumes. Development documents and inline comments use English; localization and language fixtures retain their intended languages.
- CI definitions for main/release pushes and PRs, including Linux/Windows native testing, license checks, and package verification on each event. Linux checks and native builds run concurrently; Windows checks and packaging run concurrently after the native build. Publication is restricted to release-branch pushes after those jobs succeed and consumes the packaged artifact from that workflow run. Internal Git-SHA/receipt provenance gates and workflow structure assertions have been removed; fixed upstream checksums and source/artifact audits remain. Version changes are explicit; CI does not create commits.

## Observed checks

Evidence under ignored work, artifacts, and test-results directories is local test output, not a published release. The table below preserves earlier observations; the current September 12 counts and package identities are in the completion follow-up linked above.

| Check | Observed result |
| --- | --- |
| Shared-source container isolation | Bidirectional source probe passed before/after final-source verification; ten tmpfs output masks produced no host output writes; zero named or anonymous volumes |
| Dev Container static contract | 5 tests passed; extra binds/volumes, missing masks, and output redirects rejected; every verification run allocates a fresh profile within the existing work mask |
| Linux Dev Container | UI 69; Rust native 37, AI 150, validation CLI 16, core 26, tools 19; fmt/Clippy and Rust/JS license checks passed. After correcting a test-only selector, fresh-profile real Tauri E2E passed 17 cases with 8 Windows-specific cases skipped |
| Windows Rust | AI 151, validation CLI 16, core 26, tools 21, native 37 tests passed; workspace fmt and all-target/all-feature Clippy passed. New checks cover atomic export limits/failures and update-check lifetime, cache, channel/installation changes, cancellation and concurrency |
| Frontend | 69 tests passed, including asynchronous-price identity, unsaved-settings preservation, automatic-join/original comparison, VAD pause warnings, unverified PATH candidates, manual update-check IPC/busy/error behavior, player readiness, complete source selection, sentence-pause controls, and Again scheduling; production UI build passed |
| Evaluation scripts | 33 tests passed |
| Native artifact and release contracts | 15 native-artifact, 15 release, 9 installer-audit, 6 production-probe, and 4 release-workflow tests passed, including matching source/manifest hashes, embedded plugin identity, the exact Tauri bundle-marker transformation, lifecycle evidence, and package/publication event gates; all 90 Node script/isolation tests passed after the fresh-profile harness fix |
| Native recipe replay | An empty, unmounted container completed the source build after network disconnection; ORT reconstruction again matched 1,181/1,181 observed source/header records. Synthetic local artifact identity and source-ZIP roundtrip checks passed; hosted CI was not run |
| Installer utility and Rust runtime | Source-built i686 utility passed four non-mutating function tests and required-export checks; locked dependency sources, notices, and exact Rust runtime provenance were retained for the utility and main application |
| Existing Windows native suite | An earlier 17-test run passed with real WebView2/libmpv, 20,000 subtitles, six-hour seeking, local AI-result recovery, and budget zero |
| Additional Windows functional E2E | 5/5 passed: position/audio restore, subtitle selection/version recovery, two-cue audio cards, learning management, download success/cancel/retry/cleanup |
| Source-built libmpv candidate E2E | 6/6 passed with the exact candidate DLL: the five functional cases above plus CPU AV1 decoding, visible child-window dimensions, playback progress, and seeking |
| Sentence-playback Windows E2E | One new real WebView2/libmpv case passed: persisted toggle across process restarts, joined-cue sentence stops, resume, full source-range precedence, nonadjacent-source rejection, explicit looping, and boundary refresh after subtitle edits; isolated test data restored and zero paid requests |
| Windows CI fixture integration | The updated sentence test passed with both standard offline AI fixture flags and after the real AI-recovery/media-management suites: 9 cases passed across 3 specs, with 1 optional AV1 case skipped. Strict fixed-fixture identity, absent credentials/paid approvals/dispatch, zero budget/usage, and identical before/after ledger evidence remain required |
| Final Windows native suite | The new build's first run passed four specs and 23 cases, skipped optional AV1, and failed one new case due to an invalid test selector. Correcting only that selector and rerunning the study spec with fresh data and the same executable passed all nine study cases. All 24 current cases therefore have passing evidence; no paid requests occurred. The earlier separate public-download pass remains recorded against its own executable |
| Real libmpv regression | Selected audio/position restoration, sentence pause/resume, explicit ranges, looping, and edited endpoints passed with the promoted source-built DLL and restricted DLL-directory/System32 loading |
| Learning restore with real libmpv | The generated three-second/two-audio fixture restored archived 600 ms/audio 1 over the prior 2,200 ms/audio 2 state, refreshed subtitles/sentence stops, and resisted concurrent stale ticks. Missing/removed media detached; backup and excluded local state were preserved |
| Real Silero preparation | ORT inference and FFmpeg selection-time preservation passed with the promoted CPU DLLs and separately installed System32 VC runtime. The selected 2.5-second silent fixture also persisted strict pause provenance and preserved the raw cue with a warning. The within-chunk warning path has deterministic coverage; this silent fixture does not establish spoken-audio quality |
| Real spoken/pause preparation | Two hash-pinned English utterances with a constructed four-second pause passed one real preparation/inference call. Only the synthetic interior-pause cue required review; both reference speech cues were retained, and all 159,280 selected PCM samples were unchanged. This does not establish noisy/conversational VAD or cloud quality |
| Six-hour local preparation | 345,600,000 samples, 180 chunks, approximately 122 seconds; zero cloud requests; scope/limits documented in [AI design](ai.md#six-hour-local-processing-evidence) |
| External CLI integration | Managed upstream download/verification and selected PATH executables exercised. A public YouTube import succeeded once with selected FFmpeg 9.0.1, Scoop yt-dlp 2026.08.19, and Deno 2.9.6: 81,993,350 stored bytes, 596.501 seconds, real playback/range stop/300-second seek, matching version/hash receipt, and unchanged external executables. This does not establish compatibility with every upstream build |
| Production feature exclusion | Development validation permissions and fixed E2E data are excluded from the normal application dependency graph |
| Normal Windows application | Production-feature build started with the configured credential present, zero job/budget usage, four editable model fields, and no native-player error; read-only IPC and Settings navigation sent no generation requests |
| NSIS build and extracted application | Unsigned installer built successfully; its embedded plugin, notices, corresponding-source packages, and application identity passed audit. The extracted production application passed WebView2/IPC startup, Settings, 12-second video playback, interval stop, and 7-second seeking in a Japanese/space/ampersand path. Optional tools remained uninstalled, the uniquely owned test-media registration was removed, and AI usage stayed zero. This did not execute installation, overwrite, or uninstall |

The earlier additional Windows run is recorded at work/media-functional-20260909/attempt-7. The source-built runtime run is recorded at work/native-candidate-app-20260909/attempt-1, including the loaded DLL hash and screenshots. Its DLL SHA-256 is `6d52b35d9c014b9df432b171523880505a73c3f0425226cf939e64570379be06`; its corresponding-source archive SHA-256 is `faef56ae474043a4d8edc5ce2bcdef7691e078d3df337a4c52990ee09c02dc0a`. This runtime was subsequently promoted to the intended payload after source/license review. Software decoding and silent audio output were used; real speaker output and installer lifecycle remain separate checks.

The earlier September 9 installer was 26,191,814 bytes with SHA-256 `c9f7e6ff5ab70604bf96cbe502686231525dcc9c2764937e53a441a9e7a16875`; this is not the current file at the build output path. Its audit and package evidence are preserved in `work/transcribe-production-20260912/completion-20260912/previous-packaging/`. Tauri changes the bundle marker from `UNK` to `NSS` while packaging: the audit proves the single expected marker replacement and equality of every other byte, retaining both the original build hash and the embedded application hash. That embedded application's SHA-256 was `9f2764a89104f6aff33c87f83ff53a88c4b77ae913ea7b1a3eb3a586f17941b2`; its actual playback result is recorded at work/completion-gaps-package-20260909/playback/result.json. These local native/application checks sent no generation requests and did not change the configured credential. Neither that installer nor the latest candidate has been published or exercised through its installation lifecycle.

The earlier September 9 Linux verification, retained selector failure, successful E2E rerun and mount evidence are under `artifacts/devcontainer/completion-gaps-20260909-0714/`. Only selected logs and JSON were exported; dependency and build trees stayed inside the container. That Windows full run and study rerun are under `work/completion-gaps-20260909/`; both use application SHA-256 `5bd887557e879c53e4c5204ba1bb5ac2a760b3257c59e415d0f32b2aee08d391`. The earlier public-download result is retained under `work/final-functional-20260909/attempt-1/`, with its distinct executable SHA-256 `bec3972ae13b931c7300ae804ffe40c5d7b3a078c73320251137a3c88ad3bf75`. Earlier sentence-playback integration evidence remains in `work/sentence-playback-ci-20260909/`. WebDriver screenshots show the UI but do not capture the native video child window; playback assertions use actual libmpv state and visible-surface dimensions.

The first repeated Linux run exposed retained AI fixture state in the reused disposable profile. Its failed log was preserved; the complete fresh-profile run then passed. The verification helper now allocates a new `mktemp` profile for each invocation without deleting earlier data. Five focused contract tests, two actual disjoint allocations under the container work mask, and a 90-test host Node run passed after this harness-only change. Only the three changed harness/document hashes were refreshed in the reviewed-input policy; the recipe and all other entries stayed unchanged. The later completion-gaps run covered the full Linux product suite with this fix; its separate selector correction and successful E2E rerun are recorded above.

The public import used the official yt-dlp extractor's Big Buck Bunny example, [YouTube video](https://www.youtube.com/watch?v=YE7VzlLtp-4), attributed to (c) copyright 2008, Blender Foundation / www.bigbuckbunny.org, under [CC BY 3.0](https://creativecommons.org/licenses/by/3.0/). Its saved media SHA-256 is `18775618436984541e13cf4cb5c3536409e351ffbd59077c50e5cfa47b8e8c6d`. The explicit test monitored a 256 MiB stored-byte threshold and used no retry; polling is not a hard filesystem quota or network byte cap. The test registration was removed, while its downloaded file remains only in the private test profile. The selected user-installed FFmpeg is not part of the redistribution payload.

## Current live AI evaluation

The subsequent six-request [English dialogue comparison](transcribe-en-dialogue-2026-09-12.md) concentrates on Transcribe and retains all earlier attempts unchanged. It adds USD 0.040962 to the earlier USD 1.272194 in charges and holds, for USD 1.313156 counted cumulatively. The new campaign contains exactly six serial settled attempts, without retries or a new unknown hold. Its raw and derived results remain separate from the historical results below.

The [initial 19-request report](ai-verification-2026-09-09.md) is preserved as historical evidence. It used the earlier Flash/Flash Lite configuration and retains USD 0.019505 calculated usage plus USD 0.942212 of acknowledged unknown reservation.

The current evaluation uses only the requested Transcribe and Flash 3.8 IDs. The new validation root has a USD 9.038283 lifetime cap, preserving the earlier USD 0.961717 debit from the user's cumulative USD 10 authorization. Requests are serialized and no failed request is automatically resent.

- Google model discovery returned 26 Gemini candidates. Billing metadata lookup returned HTTP 403; generation access and metadata availability are therefore treated separately. Evaluation prices were independently recorded from Google's public rates.
- Seventy Flash 3.8 text requests completed and were saved: vocabulary, translation, explanations at A2/B1/C1, cross-cue phrases, and quoted-instruction/literal preservation. The same ten T01 expressions in each language have all three proficiency levels. The final [189-item AI review](ai-quality-2026-09-09.md) preserves the original 179 scores and adds the ten previously missing B1 items. Five language/task groups pass; Japanese explanations score 1.767 against the 1.80 requirement and fail.
- Short English Flash 3.8 and Japanese Transcribe samples completed. Flash 3.8 invented speech on two seconds of digital silence; this remains a quality failure.
- The [long-audio comparison](ai-audio-quality-2026-09-09.md) covers two English inputs and one Japanese input with both models. English Transcribe recognition and the measured timing subsets are promising; Flash subtitle timing exceeds the limits. The parser now preserves equal word-time anchors without inventing durations. Eleven such anchors in the second English Transcribe response were reparsed offline into valid subtitle spans; its original state and USD 0.013182 charge remain unchanged.
- After explicit approval of the remaining audio scope and completion of the missing B1 coverage, 119 requests were processed once. The new root recorded USD 0.234001 calculated usage and no new unknown holds. Together with the historical USD 0.019505 cost and USD 0.942212 hold, USD 1.195718 is counted against the cumulative USD 10 permission. This is application accounting, not a Google invoice.
- Four requests returned unusable output despite valid usage: the equal-time English sample (subsequently reparsed offline), reversed Japanese Transcribe word times, Japanese Flash with no retained candidate, and one English boundary chunk. They were not resent or relabeled as successful production results.
- The [boundary evaluation](ai-boundary-quality-2026-09-09.md) processed 20 distinct cuts. Conservative exact matching of transport-edge fragments and continuous cue groups improved readiness from 0/20 to 7/20 and then 11/20 without another request or altered originals. Eight conflicts and one missing result remain, so the 90% automatic-stitching requirement still fails. Derived joins keep observed outer cue times and explicit provenance; they do not claim word-level timing proof. Contradictory or ambiguous responses remain reviewable.
- A subsequent offline lexical-integrity regression preserves word boundaries and numeric/symbol distinctions during matching, so expressions such as "now here"/"nowhere" and "3.5"/"35" cannot collapse. Reprocessing the retained evaluation results produced byte-identical draft/provenance evidence and the same 11/20 readiness; no request or accounting entry was added.
- Adding optional VAD pause evidence left all 20 historical boundary drafts byte-identical, including their digests, with the same 11 ready, eight conflicts, and one pending result. This compatibility check and the new local warning tests do not improve or replace the measured cloud-quality scores.
- A [local Whisper alignment experiment](ai-audio-quality-2026-09-09.md#local-whisper-alignment-experiment) preserved all 37 English Flash cue texts but worsened endpoint errors. Source inspection identified that the tested raw DTW endpoint conversion mistook a structural window boundary for acoustic onset. The conversion was not adopted; this finding does not disqualify other Whisper alignment methods. Two negative controls also showed that a successful forced-alignment call alone cannot validate spoken text. Models and dependencies stayed container-only, with zero Vertex calls or accounting changes.
- After the text evaluation, the explanation prompt gained a general guard against unsupported categorical contrasts or inferred causes/events. A shared-worker regression verifies immutable historical/current request bodies and approval bindings. This refinement has not had another paid quality evaluation, and the recorded failed score remains unchanged.
- Audio references use documented source transcripts and independent forced alignments, not claimed human timing annotations. Restricted Japanese audio remains private. The completed reports state partial coverage, rejected responses, and failed criteria explicitly.

## Unfinished completion and release requirements

1. Complete the contextual learning observations in the [accepted redesign](transcribe-product-reconsideration-2026-09-12.md), starting with the frozen ten-task saved-response set: actual listening, correctness of the saved item and human correction effort. These observations remain unset after automated transport checks. The [review-assisted Transcribe campaign](transcribe-production.md) retains its original recognition/timing thresholds and unverified conditions for those quality claims; the local draft workflow does not convert failed measurements into passes. Further paid comparison or recognition-mechanism experiments need a concrete separately approved scope. Vocabulary and explanation quality remains experimental.
2. Execute the final install/overwrite/uninstall checks in a disposable Windows profile or CI runner, including the missing-prerequisite and UAC paths. The libmpv, ORT, and installer utility payloads have reviewed dependency/license/source evidence; VC and WebView2 runtimes are separate Microsoft prerequisites. Final local package checks passed, as recorded in [native runtime status](native-runtime.md), but do not establish successful installation.
3. Complete the hosted GitHub Actions workflow with successful Linux, Windows and package jobs. Local builds and tests do not establish hosted CI or publication success; publication remains a separate release-branch action.

No test, hash check, or API response by itself establishes GPL redistribution compliance or permits publishing. The application still requires clear documentation of unsupported conditions and measured limitations before a finished release can be claimed.
